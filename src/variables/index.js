import colors from './colors';
import fonts from './fonts';
import unit from './unit';

const noImageSrc = {
  url: 'noimage',
  thumb: 'noimage thumb',
};

export {
  colors, fonts, unit, noImageSrc,
};
